import 'package:flutter/material.dart';

class BugRepo extends StatelessWidget {
  const BugRepo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      elevation: 0,
      backgroundColor: Colors.white,
    ));
  }
}
